PREFIX : <http://example.org/paper#>
# Out of the supported fragment (FILTER). Both rewriters MUST reject this
# rather than silently drop the FILTER. Used by reference/verify_gallery.py.
SELECT ?y WHERE { :Alice :knows ?y . FILTER(?y != :Bob) }
