# RQ3 byte-identity at 100M — cross-engine summary

Independently constructed flat provenance circuits over reified WatDiv **100M**; compare `circuit_sha256` against GraphDB (Java/RDF4J). Read-only engines (QLever) cover the monotone+OPTIONAL fragment (no MINUS); Oxigraph (writable) also covers MINUS where it builds.

| engine | lang | comparable cells | byte-identical |
|---|---|---|---|
